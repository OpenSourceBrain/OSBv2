harness:
  subdomain: api.accounts
  secured: false
  service:
    port: 8080
    auto: true
  deployment:
    auto: true
    port: 8080
    requests:
      memory: 120Mi
  livenessProbe:
    path: /api/live
  readinessProbe:
    path: /api/ready
  dependencies:
    hard:
      - accounts
    build:
      - cloudharness-base
      - cloudharness-flask